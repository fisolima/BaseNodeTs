/// <reference path="../../typings/index.d.ts" />

import * as chai from 'chai';

describe('Sample test', () => {
  it('should return true', () => {
    const result = 'killpippo';
    chai.expect(result).to.equal('killpippo');
  });
});